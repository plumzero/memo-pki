数字信封
	数字信封加密过程
		明文加密：发送方通过对称密钥将明文加密成密文；
		密钥打包：发送方将对称密钥用接收方的公钥加密；
		封装：发送方将密文和加密后的对称密钥发送给接收方。
	数字信封解密过程
		获取密钥：接收方利用私钥获得对称密钥；
		密文解密：利用对称密钥对密文进行解密。
	发送方和接收方每进行一次通信都可以看作是一个信封加密和解密的过程。
	随机对称密钥在每个执行过程是不断变化的。